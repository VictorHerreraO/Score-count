<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for light mode -->
    <style name="Theme.Scorecount" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Splash screen attributes for Android 12+ -->
        <item name="android:windowSplashScreenBackground" tools:targetApi="s">@color/ic_background</item>
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="s">@mipmap/ic_launcher_round</item>
    </style>
</resources>